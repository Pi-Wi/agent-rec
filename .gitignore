**/__pycache__
*.pyc
*.pyo
.pytest_cache/
*.egg-info/
dist/
.env
venv/
.venv/
# Recorded corpora are user data and ignored by default. The curated demo
# corpus/ is the deliberate exception — it ships with the repo (force-added
# past this rule, `git add -f corpus/`) so the README "Try it now" demo works
# from a fresh checkout. New cassettes a user records into corpus/ stay ignored.
corpus/
corpus-support/
corpus-imported/
corpus-imported-src/
reports/
migration-report__*
CLAUDE.local.md
.claude